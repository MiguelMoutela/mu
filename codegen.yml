schema: src/graphql/**/*.ts
overwrite: true
watch: true
require:
    - ts-node/register
generates:
    ./src/types/types.ts:
        plugins:
            - typescript-common
            - typescript-server
            - typescript-resolvers
            - typescript-mongodb
