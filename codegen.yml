overwrite: true
schema: "./src/graphql/**/*.ts"
documents: null
generates:
    src/types/types.d.ts:
        plugins:
            - "typescript-common"
            - "typescript-resolvers"
            - "typescript-server"
